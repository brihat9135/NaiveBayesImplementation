In order to run the code, we have to download nlkt stopwords: nlkt.download() worked for me to download them
Both the Naive Bayes algorithm and data cleaning are done inside the DataProcess class, this made things lot easier to run 
path location should be changed depending where the data files are kept

Python libraries are used such as Sklearn, pandas, numpy. etc. 


